# Michaelis Menten
    # A reaction model of a michaelis_menten reaction
    # Includes basic initial condition, assignments, and reaction
    %% ReactionModel@2
    % components

    # parameters
    km := 0.2
    kcat := 0.5
    e:= 1.0
    b0 := 1

    # compartment
    v~3 = 2

    a@v *= 1
    b@v *= b0

    a -> b; km=km, kcat=kcat, e=e