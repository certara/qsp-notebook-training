# Transport
    # A reaction model of a transport reaction

    %% ReactionModel@2
    % components

    # parameters
    pdist := 0.50
    tdist := 0.2
    b0 := 2

    # compartment
    v~3 = 5
    w~3 = 2

    a@v *= 1
    b@w *= b0

    a <-> b; pdist = pdist, tdist = tdist